import { special } from 'project-name/helpers/special';
import { module, test } from 'qunit';

module('Unit | Helper | special');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = special([42]);
  assert.ok(result);
});
